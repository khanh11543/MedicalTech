package com.q2k.meditech.entity.enums;

public enum AuditActionType {
    CREATE,            // Tạo mới
    UPDATE,            // Cập nhật
    DELETE,            // Xóa
    LOGIN,             // Đăng nhập
    LOGOUT,            // Đăng xuất
    SENSITIVE_ACCESS   // Truy cập dữ liệu nhạy cảm
}
